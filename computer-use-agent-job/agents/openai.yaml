interface:
  display_name: "Computer Use Agent Job"
  short_description: "Fill, learn, and sync reusable Chinese ATS workflows."
  default_prompt: "Use $computer-use-agent-job to fill or audit this job application, reuse ATS-specific lessons, and sync newly verified rules."
