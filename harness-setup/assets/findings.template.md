# Findings — distilled durable lessons

Lessons that have **graduated** out of the raw log (`log/`) — each is a reusable
rule, not a one-off observation. When a lesson here can be expressed as a test,
promote it to a regression fixture, link it, and prune the log entries it came from.

Maintenance rules:

- Only record **confirmed** lessons; unverified hypotheses stay in `log/`.
- Express each as reusable behavior, not a one-time incident.

<!-- Seed with real graduated lessons as they emerge. Example shape:

## {{AREA}}

1. **{{Short rule.}}** {{Why / when it applies.}} _(fixture: {{path or "TBD"}})_
-->

_(empty — populate by graduating lessons from `log/`.)_
